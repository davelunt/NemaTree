# Main entrypoint of the workflow. 
# Please follow the best practices: 
# https://snakemake.readthedocs.io/en/stable/snakefiles/best_practices.html,
# in particular regarding the standardized folder structure mentioned there. 
configfile: "config/config.yaml"

# import pandas as pd

# samples_df = pd.read_table(config['sample_file'], header = None)  # Pull in data from tsv

# SAMPLES = list(samples_df[0])  # create list from first column of dataframe

# SAMPLES=["2012_114"] 
SAMPLES=["alvarez"] 

rule all:
    input:
        expand("resources/samples/{sample}_validated.fas", sample=SAMPLES),
        "results/reporting/seqkit/initial_seqkit_report.md",
        expand("results/mafft/{sample}_mafft.fas", sample=SAMPLES),
        # expand("results/reporting/{sample}_checkaddseqs_log.txt", sample=SAMPLES),
        expand("results/cialign/{sample}_mafft_cialign_cleaned.fasta", sample=SAMPLES),
        # expand("results/iqtree/{sample}/{sample}_mafft_cialign_iqtree.treefile", sample=SAMPLES),
        # expand("results/reporting/toytree/{sample}_mafft_iqtree.html", sample=SAMPLES),
        # "results/reporting/amas/2012_145_mafft_amas.tsv",
        # "output_a.txt" if config['run_rule_a'] else [],

include: "rules/qc.smk",
include: "rules/alignment.smk",
include: "rules/trees.smk",
include: "rules/reports.smk",
